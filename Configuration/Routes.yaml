#                                                                        #
# Default subroutes configuration for the Training package               #
#                                                                        #

-
  name: 'Fallback rule'
  uriPattern: '(login)'
  defaults:
    '@controller': 'Login'
    '@action': 'index'
